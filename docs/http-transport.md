# OneBot HTTP ????

> ??????? OneBot HTTP API + webhook?????? WebSocket ??????

## ????

- ?? OneBot / NapCat ????? HTTP API?
- ????????? webhook ???????
- ????????? QQ ???????????????????????

## ???

? `channels.qq` ???????????

- `transport`??????? `http` ??? HTTP ??????? `ws`?
- `httpUrl`?OneBot HTTP API ????? `http://127.0.0.1:3000`?
- `httpWebhookPath`??????? OneBot ??? webhook ?????????? `/plugins/qq/<accountId>/onebot`?
- `httpWebhookToken`?HTTP webhook ??? token??????? `accessToken`?
- `accessToken`?????? webhook ????? token?? `httpWebhookToken` ?????

## ????

```json
{
  "channels": {
    "qq": {
      "transport": "http",
      "httpUrl": "http://127.0.0.1:3000",
      "httpWebhookPath": "/plugins/qq/default/onebot",
      "httpWebhookToken": "your_webhook_token",
      "accessToken": "your_token",
      "requireMention": true
    }
  },
  "plugins": {
    "entries": {
      "qq": { "enabled": true }
    }
  }
}
```

## NapCat / OneBot ???????

HTTP ??????????????

1. **?? API**
   - OpenClaw QQ ?? `httpUrl` ?? `POST <httpUrl>/<action>` ???
   - ?? OneBot ??????? HTTP API?????? `Authorization: Bearer <token>` ???

2. **???? webhook**
   - OneBot ??????/notice/request ???????? `httpWebhookPath`?
   - ????????????
     - `Authorization`
     - `x-onebot-token`
     - `x-access-token`

## ????

- ??? `transport` ???????? WebSocket ???
- HTTP ?????????????????????????????????????
- ??????????????????????? webhook ???????? handler?

## ????

### 1. ??????????

?????

- OneBot ???????????? `httpWebhookPath`
- webhook token ??? `httpWebhookToken` / `accessToken` ??
- ??????? `POST`

### 2. ???????????

?????

- `httpUrl` ???? OpenClaw ??????
- OneBot HTTP API ???? `POST /send_private_msg`?`POST /send_group_msg` ??? action
- `accessToken` ??? OneBot ???????

### 3. ??? WebSocket ??

??????????

1. ???? `accessToken`
2. ? `transport` ?? `http`
3. ?? `httpUrl`
4. ?? OneBot ????? `httpWebhookPath`
5. ?? `openclaw gateway restart`

## ????

- `docs/config-reference.en.md`
- `deploy/napcat/README.md`
- PR: `#36`
